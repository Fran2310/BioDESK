<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Resultados de Examen - {{request.medicTestName}}</title>
    <style>
        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            color: #2e2e2e;
            margin: 0;
            font-size: 10pt;
            background-color: #ffffff;
        }

        .page {
            padding-top: 3cm;
            padding-bottom: 2.5cm;
            margin: 0;
        }

        .header, .footer {
            position: fixed;
            left: 0;
            right: 0;
            width: 100%;
        }

        .header {
            top: 0;
            background: linear-gradient(to right, #1c5a6b, #58a49c);
            color: white;
            padding: 12px 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 3px solid #ffffff;
            box-sizing: border-box;
        }

        .lab-info h1 {
            margin: 0;
            font-size: 16pt;
            text-transform: uppercase;
        }

        .lab-info p {
            margin: 2px 0;
            font-size: 9pt;
        }

        .lab-logo {
            height: 60px;
            max-width: 150px;
            object-fit: contain;
            margin-left: 20px;
        }

        .footer {
            bottom: 0;
            background-color: #f2f2f2;
            color: #444;
            border-top: 1px solid #ccc;
            font-size: 8pt;
            text-align: center;
            line-height: 1.4;
            padding-top: 6px;
            padding-bottom: 6px;
            box-sizing: border-box;
        }

        .footer .phones {
            margin-top: 3px;
            font-weight: bold;
            color: #333;
        }

        .section {
            margin: 0 20px 25px 20px;
        }

        .section h2 {
            font-size: 12pt;
            color: #1c5a6b;
            border-bottom: 2px solid #58a49c;
            padding-bottom: 5px;
            margin-bottom: 10px;
            text-transform: uppercase;
        }

        .info-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 6px 30px;
        }

        .info-grid p, .section p {
            margin: 4px 0;
        }

        .results-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 10px;
        }

        .results-table th, .results-table td {
            border: 1px solid #ccc;
            padding: 6px 8px;
            text-align: left;
        }

        .results-table th {
            background-color: #d8f1ef;
            color: #1c5a6b;
            font-weight: bold;
        }

        .results-table td:nth-child(2) {
            font-weight: bold;
            color: #1a3f4d;
        }

        h1.main-title {
            text-align: center;
            font-size: 18pt;
            color: #1c5a6b;
            margin: 0 20px 30px 20px;
        }
    </style>
</head>
<body>

    <div class="header">
        <div class="lab-info">
            <h1>{{lab.name}}</h1>
            <p>{{lab.dir}}</p>
            <p>RIF: {{lab.rif}}</p>
        </div>
        {{#if lab.logoPath}}
            <img src="{{lab.logoPath}}" alt="Logo del Laboratorio" class="lab-logo">
        {{/if}}
    </div>

    <div class="footer">
        Página <span class="pageNumber">1</span> de <span class="totalPages">1</span><br>
        <span>{{lab.name}}</span>
        <div class="phones">
            Teléfonos: {{#each lab.phoneNums}}{{this}}{{#unless @last}} · {{/unless}}{{/each}}
        </div>
    </div>

    <div class="page">
        <h1 class="main-title">Resultados de Examen Médico</h1>

        <div class="section">
            <h2>Datos del Paciente</h2>
            <div class="info-grid">
                <p><strong>Nombre Completo:</strong> {{patient.fullName}}</p>
                <p><strong>Cédula de Identidad:</strong> {{patient.ci}}</p>
                <p><strong>Edad:</strong> {{patient.age}}</p>
                <p><strong>Género:</strong> {{patient.gender}}</p>
            </div>
        </div>

        <div class="section">
            <h2>Datos del Examen</h2>
            <div class="info-grid">
                <p><strong>Tipo de Examen:</strong> {{request.medicTestName}}</p>
                <p><strong>Fecha de Emisión:</strong> {{request.completedAtFormatted}}</p>
                <p><strong>Solicitud Nro:</strong> {{request.id}}</p>
                <p><strong>Aprobado por:</strong> {{systemUser.fullName}}</p>
            </div>
        </div>
        
        <div class="section">
            <h2>Resultados</h2>
            <table class="results-table">
                <thead>
                    <tr>
                        <th>Prueba</th>
                        <th>Resultado</th>
                        <th>Unidades</th>
                        <th>Rango de Referencia</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each results}}
                    <tr>
                        <td>{{this.propertyName}}</td>
                        <td>{{this.value}}</td>
                        <td>{{this.unit}}</td>
                        <td>{{this.referenceRange}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>

        {{#if request.observation}}
        <div class="section">
            <h2>Observaciones</h2>
            <p>{{request.observation}}</p>
        </div>
        {{/if}}

    </div>
</body>
</html>
